{
  "name": "sonicpixels-v2",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Sonic Pixels — a one-page, audio-reactive WebGL portfolio.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "generate:media": "node scripts/generate-media.mjs"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  },
  "dependencies": {
    "three": "^0.169.0"
  }
}
